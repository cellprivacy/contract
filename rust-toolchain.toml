# Pinned rather than "stable" so fmt and clippy behave identically for every
# contributor and in CI. A floating channel means a lint that is clean on one
# machine is a hard error on another.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
targets = ["wasm32v1-none"]
